const myNumber = 12.40;
// const output = Math.abs(myNumber);

/* const myNumber = 21.95; */
// const output = Math.ceil(myNumber);
// const output = Math.floor(myNumber);
const output = Math.round(myNumber);

// const output = Math.random() * 6;
// const rounded = Math.round(output);
console.log(output);

/* for (let i = 0; i <= 30; i++) {
  const output = Math.random() * 6;
  const rounded = Math.round(output);
  console.log(rounded);
}

const number = 589.2356; */
// Math.floor
// Math.ceil
// Math.round
/* 
const random = Math.random();
const between10 = random * 100;
const rounded = Math.round(between10);
console.log(rounded);
 */

/* const selected = [];
for (let i = 0; i < 10; i++) {
  const random = Math.random() * 100;
  const picked = Math.round(random);
  if (selected.indexOf(picked) == -1) {
    // -1 is here for the reason of if element not in index
    selected.push(picked);
  } else {
    console.log("found duplicate", selected, picked);
  }
}
console.log(selected); */

